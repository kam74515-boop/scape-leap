# Optional ecology images

The ecology mock data references files in this directory. The application renders
deterministic gradient placeholders when an image is absent, so clean clones and
CI builds remain self-contained.

For local demos with the full image library, copy the licensed `product_*` assets
into this directory. Do not replace this directory with an absolute symlink or
commit unlicensed source images.
